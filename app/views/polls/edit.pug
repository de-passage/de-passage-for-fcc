extends ../layout.pug

block title
  | #{poll.name} | Edit

block content
  form(method="post", action="/voting-app/#{poll.name}?_method=PUT")
    input(type="text", name="name", placeholder="Name", required=true)
    textarea(name="description", placeholder="Optional description", required=true)
    #option-input
    input(type="submit", value="Save poll")
